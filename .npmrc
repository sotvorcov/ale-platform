# Dev dependencies are build tooling (tsc, eslint, prettier, tsx).
# The runtime sets NODE_ENV=production, which would otherwise make npm skip
# them. Force them in so `npm install` works the same locally and in CI.
include=dev
